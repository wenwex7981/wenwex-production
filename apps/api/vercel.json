{
    "version": 2,
    "buildCommand": "npx prisma generate --schema=../../packages/database/prisma/schema.prisma",
    "builds": [
        {
            "src": "src/index.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/index.ts"
        }
    ]
}