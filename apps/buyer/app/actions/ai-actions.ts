'use server';

/**
 * WENWEX AI - DEACTIVATED TEMPORARILY
 * Waiting for new API credentials.
 */

export async function chatWithWenwexAI(query: string) {
    return {
        success: false,
        reply: "WENWEX AI is currently undergoing scheduled maintenance to improve performance. Please check back shortly!"
    };
}
