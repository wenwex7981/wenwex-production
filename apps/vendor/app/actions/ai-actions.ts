'use server';

/**
 * WENWEX VENDOR AI - DEACTIVATED TEMPORARILY
 */

export async function generateServiceDescriptionAction(title: string, category: string, features: string[]) {
    return {
        success: false,
        error: "One-Click AI Description is currently in maintenance mode. Please write your description manually for now."
    };
}
